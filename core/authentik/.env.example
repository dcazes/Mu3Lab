# Pin an explicitly reviewed Authentik release; never use :latest here.
AUTHENTIK_TAG=2026.5.0
AUTHENTIK_SECRET_KEY=generate_during_bootstrap
AUTHENTIK_POSTGRESQL__PASSWORD=generate_during_bootstrap
# The first administrator is created in Authentik's own UI. Mu3Lab never
# accepts, stores, or logs that password.
