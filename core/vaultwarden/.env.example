# Generated during bootstrap. Store only in /srv/mu3lab/secrets, mode 0600.
ADMIN_TOKEN=generate_during_bootstrap
SIGNUPS_ALLOWED=true
# Set by the ingress renderer after the canonical tailnet hostname is known.
DOMAIN=https://mu3lab.example.ts.net:8081
