# Mu3Lab :: ctl/requirements.txt
# WHAT:  Python dependencies for the control plane backend.
# WHY:   Installed into the repo-root .venv by install.sh. Deliberately small:
#        web server + docker SDK + yaml + system metrics + http client.
#        No fastmcp (MCP server is Step 2 work, not this build).
# DEBUG: `.venv/bin/pip install --dry-run -r ctl/requirements.txt` resolves
#        versions without installing anything.

fastapi>=0.115
uvicorn[standard]>=0.30
docker>=7.1
pyyaml>=6.0
psutil>=6.0
httpx>=0.27
cryptography>=43.0
