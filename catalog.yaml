# Mu3Lab bundle definitions. Per-service copy and deployment facts are generated
# from the typed services.yaml manifest so the UI cannot drift from reality.
schema_version: 1
profiles:
  - id: core-suite
    name: Core suite
    description: "The supported Mu3Lab AI slice: local embeddings, external-provider routing, and private chat."
    services: [ollama, freellmapi, litellm, open-webui]
